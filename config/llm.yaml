# Pinned LLM setup (SPEC 8.1, 8.4). Changing model or CLI version is a model change:
# new signal, shadow mode first.
model: claude-opus-5-5
cli_version: "2.1.236"
prompt_version: v1
daily_call_budget: 40
calls_per_session: 10
