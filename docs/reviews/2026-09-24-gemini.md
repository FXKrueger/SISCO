# Review 1: independent harness and strategy review

Reviewer: Gemini 3.8 Flash (High) via `agy`, read-only, with all files in the prompt. The reviewer could not run anything. Raw output: [2026-09-24-gemini-raw.md](2026-09-24-gemini-raw.md). Codex was not available: the ChatGPT account has no access to the model.

Every finding below was checked by Claude (the author) before any change. Disposition in the last column.

| # | Sev. (reviewer) | Finding | Checked | Disposition |
|---|---|---|---|---|
| 1 | P0 | The $5B top cost tier is unreachable, so BTC and ETH get charged 15 bp | **Wrong as stated.** BTC's median daily Binance volume is $14B (1 bp tier on 85% of days), ETH's $6.7B (73%). **A real problem sits next to it:** SOL, XRP and DOGE were charged 15 bp, measured 3-12 bp; LINK, ADA, AVAX 30 bp, measured ~15 bp. 57% of B-001's coin-days were in the 15 bp tier. | Fixed: new tiers from 168 book samples: 1 / 8 / 15 / 40 bp |
| 2 | P0 | Null test: no stop or target, so unbounded payoffs; same-bar exits give 0 | **Confirmed.** A 10% move against a 0.5% stop gave ±20 R null trades. | Fixed: null trades now run through the engine with the same stop and target distance, time limit and costs, but a random coin and side |
| 3 | P1 | Funding charged at the settlement at the end of a bar after an intrabar stop | **Confirmed.** Small effect (one funding rate per affected trade). | Fixed: stops and targets exclude a settlement at the bar end; time exits at the close still include it |
| 4 | P1 | A limit filled inside a bar ignores a target touched in the same bar unless the bar closes beyond it | **Deliberate and conservative:** the high may come before the fill. The limit canary shows no net bias (+0.008 ± 0.026 R/trade over 3692 trades). | Kept |
| 5 | P1 | Pending limit orders use slots of the 5R limit; the order of the universe decides who gets them | **True, but not arbitrary:** the universe is sorted by liquidity, so the most liquid coins get priority. Counting pending orders against 5R matches live risk (they can all fill). | Kept, P3 |
| 6 | P1 | Reaction delay keeps stale stops: a late fill can be beyond the stop | **Confirmed** as a weakness of the test design. | Fixed: a late market entry whose stop is already through is skipped (nobody would place it) |
| 7 | P2 | `PITView` ignores a past `end` argument | **Confirmed.** No leak (it still stops at t), but wrong behavior. | Fixed |
| 8 | P2 | Pre-registration checks only `spec.yaml` and `strategy.py`, not helper files | **Confirmed.** An agent could change behavior in an unregistered helper. | Fixed: every file in the strategy folder except reports must match `origin/main`, and all `.py` files are linted |
| 9 | P2 | The regex lint can be bypassed (`__import__`, `sys.modules`) | **Confirmed.** Known limit of a token lint. | More patterns added; the verifier's review remains the second line |
| 10 | P2 | B-001 closes after 7 days and re-enters: repeated costs | **True,** but it follows the system's holding frame (4 h to 7 days, SPEC 3). Any 7-day strategy pays this. | Kept; noted in the B-001 report |
| 11 | P3 | One missing day drops a coin from the universe for 30 days | **Confirmed.** | Fixed: needs 20 of 30 days |
| 12 | P3 | The exit cost tier uses liquidity at signal time | **Confirmed.** | Fixed: liquidity at exit time |

## Effect on results

- **H-001: the conclusion stands.** Fibonacci = random and gross ≈ 0 do not depend on costs or the null test. The reviewer agrees ("YES, the rejection is scientifically valid"). No re-run, so no trial budget is used.
- **B-001 must be re-measured.** The cost tiers were about 2x too high for mid-sized coins. The spec budget is raised from 1 to 2. After merge: one re-run. Its 2x-cost Sharpe then replaces -0.21 as the gate 3 bar.
